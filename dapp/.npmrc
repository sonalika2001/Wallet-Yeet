# Hoist all transitive deps to top-level node_modules so Next.js's webpack
# resolver finds peer deps like @metamask/sdk (used internally by wagmi /
# RainbowKit). Without this, pnpm's strict mode hides them in .pnpm/ and
# Next's "Module not found" error fires.
shamefully-hoist=true
